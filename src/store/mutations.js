/**
 * Root Mutations
 *
 * Here is place of all available types,
 * this also can be split into separated files,
 * but because we just need little types,
 * we can put all into one file only.
 */
export const MARK_AS_FAVORITE = 'MARK_AS_FAVORITE'
